
buildscript {
    repositories {
        flatDir {
            dirs 'plugins/'
        }
    }


    dependencies {
        classpath name: 'dependency-graph-plugin-0.0.1'
    }
}

import dependencyGraphPlugin.DependencyGraphPlugin
apply plugin: DependencyGraphPlugin

task("clean")